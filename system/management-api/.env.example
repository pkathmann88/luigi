# NEVER COMMIT THIS FILE TO GIT!
# This file contains sensitive credentials and configuration

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=8443
HOST=0.0.0.0

# Authentication (REQUIRED)
# 
# NOTE: These credentials are automatically set during installation!
# The setup script (setup.sh) will prompt you for username and password,
# then apply them to BOTH frontend and backend automatically.
# 
# Manual editing is only needed if you want to change credentials after installation.
# If you change these values manually, you must rebuild the frontend:
#   1. Update this file with new credentials
#   2. Run: sudo ./setup.sh build (will prompt for same credentials)
#   3. Run: sudo systemctl restart management-api
#
# Default values shown below are only used if setup script is skipped:
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme123

# HTTPS/TLS Configuration (REQUIRED)
USE_HTTPS=true
TLS_CERT_PATH=/home/pi/certs/server.crt
TLS_KEY_PATH=/home/pi/certs/server.key

# IP Whitelist (optional - comma-separated IPs)
# Leave empty to allow all local network IPs
# Example: ALLOWED_IPS=192.168.1.100,192.168.1.101
ALLOWED_IPS=

# Rate Limiting
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_FILE=/var/log/luigi/management-api.log
LOG_LEVEL=INFO
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5
AUDIT_LOG_FILE=/var/log/luigi/audit.log

# System Paths
MODULES_PATH=/home/pi/luigi
CONFIG_PATH=/etc/luigi
LOGS_PATH=/var/log
